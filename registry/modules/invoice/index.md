---
title: "Invoice"
description: "InvoiceModule provides invoice functionality. Repositories are injected via the ModuleSet provider map to avoid duplication."
type: "module"
author: "Kthulu Core"
stars: 0
icon: "Box"
---

# Invoice

InvoiceModule provides invoice functionality. Repositories are injected via the ModuleSet provider map to avoid duplication.

## Features

- HTTP API
- Domain Logic



## Configuration

The module is configured via environment variables:

| Variable | Description |
|----------|-------------|
| - | No environment variables detected |


## Installation

Add this module to your project:

```bash
kthulu add module invoice
```

## Components

This module provides the following components to the application:

- **Backend**: Go module wired with Uber Fx.

