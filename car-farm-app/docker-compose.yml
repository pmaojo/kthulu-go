version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=localhost
      - DB_PORT=0
      - DB_NAME=car-farm-app
      - DB_USER=admin
      - DB_PASSWORD=password
    networks:
      - car-farm-app-network


volumes:
  car-farm-app-data:

networks:
  car-farm-app-network:
    driver: bridge
