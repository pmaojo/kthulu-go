version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=localhost
      - DB_PORT=0
      - DB_NAME=tournament-app
      - DB_USER=admin
      - DB_PASSWORD=password
    networks:
      - tournament-app-network


volumes:
  tournament-app-data:

networks:
  tournament-app-network:
    driver: bridge
