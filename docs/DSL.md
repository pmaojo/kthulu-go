# Project Blueprint DSL

Kthulu introduces a declarative approach to software architecture using **Project Blueprints**. A Blueprint is a YAML file (`kthulu-plan.yaml`) that defines the requirements, structure, and features of your application before a single line of code is written.

## The Workflow

1.  **Plan**: Describe your project requirements and desired features.
2.  **Refine**: Edit the generated YAML to fine-tune the architecture.
3.  **Generate**: (Coming Soon) Feed the blueprint into the generation engine to scaffold the entire solution.

## Commands

### `kthulu plan`

Generates an initial blueprint based on a template.

```bash
# Create a plan for an e-commerce system
kthulu plan my-shop --template=ecommerce --features=payment,cart

# Output: kthulu-plan.yaml
```

### Requirement Management

You can manage high-level requirements directly within the CLI, which are stored in the blueprint.

```bash
# Add a requirement
kthulu plan req add "Users must be able to reset passwords via email" --priority=High

# List requirements
kthulu plan req list
```

## Blueprint Format (`kthulu-plan.yaml`)

The blueprint schema allows you to define every aspect of the system.

```yaml
name: my-shop
description: "Generated by Kthulu Planner"
template: ecommerce

# Core Tech Stack
database: postgres # postgres, sqlite, mysql
frontend: none     # none, react, gth (Go-Templ-HTMX)
auth: jwt          # jwt, oauth2, session

# Feature Modules (Vertical Slices)
features:
  - product
  - cart
  - order
  - payment

# Module Configuration (Fine-grained control)
modules:
  product:
    fields:
      - name
      - price
      - sku
      - description

# Project Requirements
requirements:
  - id: REQ-1
    title: "Users must be able to reset passwords via email"
    priority: High
    status: Pending
    created: "2023-10-27T10:00:00Z"
```

## Why use Blueprints?

- **Architecture as Code**: Version control your architecture decisions.
- **AI Context**: The blueprint serves as the "Source of Truth" for AI agents, allowing them to understand the *intent* of the project, not just the code.
- **Collaboration**: Stakeholders can review the `requirements` section of the YAML to ensure alignment before implementation starts.
