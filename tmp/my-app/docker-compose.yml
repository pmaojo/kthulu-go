version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=localhost
      - DB_PORT=0
      - DB_NAME=my-app
      - DB_USER=admin
      - DB_PASSWORD=password
    networks:
      - my-app-network


volumes:
  my-app-data:

networks:
  my-app-network:
    driver: bridge
