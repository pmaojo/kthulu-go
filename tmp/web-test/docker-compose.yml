version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=localhost
      - DB_PORT=0
      - DB_NAME=web-test
      - DB_USER=admin
      - DB_PASSWORD=password
    networks:
      - web-test-network


volumes:
  web-test-data:

networks:
  web-test-network:
    driver: bridge
