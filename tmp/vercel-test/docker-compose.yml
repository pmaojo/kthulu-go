version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=localhost
      - DB_PORT=0
      - DB_NAME=vercel-test
      - DB_USER=admin
      - DB_PASSWORD=password
    networks:
      - vercel-test-network


volumes:
  vercel-test-data:

networks:
  vercel-test-network:
    driver: bridge
