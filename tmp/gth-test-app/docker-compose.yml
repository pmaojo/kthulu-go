version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=localhost
      - DB_PORT=0
      - DB_NAME=gth-test-app
      - DB_USER=admin
      - DB_PASSWORD=password
    networks:
      - gth-test-app-network


volumes:
  gth-test-app-data:

networks:
  gth-test-app-network:
    driver: bridge
