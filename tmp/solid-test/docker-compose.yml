version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=localhost
      - DB_PORT=0
      - DB_NAME=solid-test
      - DB_USER=admin
      - DB_PASSWORD=password
    networks:
      - solid-test-network


volumes:
  solid-test-data:

networks:
  solid-test-network:
    driver: bridge
